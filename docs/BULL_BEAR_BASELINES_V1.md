# Bull / bear Stage 2 baseline summary

This is a descriptive same-origin development summary over the existing August
ghost-candle ledger (`data/ghost_candles/experiments/scores.parquet`): 5,586
origins across 21 sessions. The fixed label is `close_return_pm_10bp_v1`.

The observed labels were bull 198, neutral 5,155 and bear 233. The median path
of every engine is classified with the same threshold for a hard descriptive
comparison. This is not a probability score: the existing ledger stores binary
up probabilities, not three-class probabilities, so the artifact explicitly
marks `probability_status` as `not_available`.

The unchanged-price baseline has five-minute MAE $0.300862. Historical analogue
is $0.306623, Kronos Small $0.311081 and Kronos Mini $0.319270. Hard class
accuracy is dominated by the 92.3% neutral prevalence and must not be read as
evidence of directional skill. July is not read or retuned.

Generated by `bull_bear_baselines.run`; output is
`data/bull_bear_truth_v1/baseline_summary.json`. The next bounded task is to
add a genuine same-origin three-class distribution for the simple baselines and
Kronos paths, then score multiclass Brier/log loss. Jev and order flow remain
deferred.
